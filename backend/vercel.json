{
  "version": 2,
  "installCommand": "npm install && npx prisma generate && npx prisma migrate deploy",
  "builds": [
    {
      "src": "index.js",
      "use": "@vercel/node"
    }
  ],
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.js" }
  ]
}
